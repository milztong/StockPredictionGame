# Database — reads from env var if set, falls back to local Supabase URL
spring.datasource.url=${SPRING_DATASOURCE_URL: postgresql://postgres:[YOUR-PASSWORD]@db.dgjvorttyxvfxgypmvio.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME: postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD: Januarsechsundzwanzig2000!}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.use_get_generated_keys=true
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# Alpha Vantage
alphavantage.api.key=${ALPHAVANTAGE_API_KEY: 6U06P1I29IBTB6L4}

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiry.ms=${JWT_EXPIRY_MS:86400000}

# CORS
frontend.url=${FRONTEND_URL:http://localhost:3000}

# Server
server.port=8080
